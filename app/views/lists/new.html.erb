<div class="mt-5 d-flex justify-content-around align-items-center ">
  <h1 class="h1 mx-auto">Add new list</h1>
  <div class="mx-auto">
    <%= simple_form_for @list do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
